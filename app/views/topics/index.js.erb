$("#paginate_<%= @topic.id %>").html("<%= j(render partial: 'index',locals:{topic_id: @topic.id, comment: Kaminari.paginate_array(@topic.comments.reverse).page(@page).per(3)})%>");
console.log("<%= @topic.id %>")
